export default function CartPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold text-gray-900">Cart</h1>
      <p className="mt-2 text-gray-500">
        Cart page — added products, item count, and total price will be
        displayed here.
      </p>
    </div>
  );
}
